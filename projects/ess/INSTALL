Easy Service Scanner v0.3.6 by Peter Postma <peter@pointless.nl>

To compile the program:
# make

You can specify -DIPV4_DEFAULT to use IPv4 only by default.
See the Makefile for an example.

If you want to install the program then do:
# make install

See manpage ess(1) for documentation.
